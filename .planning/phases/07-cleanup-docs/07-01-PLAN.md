---
phase: "07"
plan: "01"
type: cleanup
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-04]
---

# Phase 7 Plan 01: Cleanup & Documentation

## Objective
Remove old TypeScript code, update .gitignore for Rust, rewrite README.md and skill.md to reflect the Rust CLI.

## Tasks

### Task 1: Remove old TypeScript code and update .gitignore
type="auto"
- Delete: clawduel-cli.ts, tsconfig.json, package.json, package-lock.json, dist/, node_modules/
- Rewrite .gitignore for Rust project (target/, *.pdb, IDE files, OS files)

### Task 2: Update README.md
type="auto"
- Rewrite for Rust CLI: cargo install, wallet create/import, all commands with examples
- Multi-agent support, interactive shell, fight loop, agent integration

### Task 3: Update skill.md
type="auto"
- Reference Rust binary instead of npm package
- Update installation (download binary or cargo install)
- Update command examples (wallet create instead of init)

## Verification
- No TypeScript files remain in project root
- .gitignore has Rust entries, no Node entries
- README references Rust/cargo, not npm/node
- skill.md references binary download, not npm install

## Success Criteria
- All old TS artifacts removed
- Documentation accurately reflects the Rust v2.0 CLI
