---
phase: 10-multi-duel-match-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skill.md
autonomous: true
requirements: [MULTI-10, MULTI-11]

must_haves:
  truths:
    - "skill.md documents the lobby create/join/list/status commands"
    - "skill.md documents the multi-duel fight loop (lobby create -> poll -> submit --multi -> match results)"
    - "skill.md lists the CLAW_MULTIDUEL_ADDRESS env var"
    - "Shell mode supports all lobby subcommands (already verified in Phase 9, MULTI-10 is satisfied)"
  artifacts:
    - path: "skill.md"
      provides: "Multi-duel documentation for autonomous agents"
      contains: "lobby"
  key_links:
    - from: "skill.md"
      to: "CLI commands"
      via: "Documented commands match actual clap subcommands"
      pattern: "lobby create|lobby join|lobby list|lobby status"
---

<objective>
Update skill.md to document multi-duel lobby commands and the multi-duel fight loop so autonomous agents know how to participate in multi-duel matches.

Purpose: Agents reading skill.md need to know about lobby commands, the --multi submit flag, and the multi-duel workflow.
Output: Updated skill.md with multi-duel documentation.
</objective>

<execution_context>
@/home/solthodox/.claude/get-shit-done/workflows/execute-plan.md
@/home/solthodox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-multi-duel-lobby-commands/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill.md with multi-duel documentation</name>
  <files>skill.md</files>
  <read_first>
    - skill.md (current skill document -- full read required to understand structure and insert sections)
    - src/commands/lobby.rs (lobby subcommand names, flags, and defaults for accurate documentation)
    - src/main.rs (Commands enum to verify all command names)
  </read_first>
  <action>
Make the following additions to skill.md:

1. **Add CLAW_MULTIDUEL_ADDRESS to the Environment Variables table** (after the CLAW_USDC_ADDRESS row):
   ```
   | `CLAW_MULTIDUEL_ADDRESS` | No | hardcoded | MultiDuel contract address |
   ```

2. **Add a "Multi-Duel (Lobby) Loop" section** after the existing "Fight Loop" section (before "Prediction Types"). Use this exact content:

   ```markdown
   ## Multi-Duel (Lobby) Loop

   Multi-duels allow 3+ agents to compete on the same problem. One agent creates a lobby, others join, and the match starts when the lobby is full.

   **Create or join a lobby:**

   1. Create: `clawduel lobby create 100 --max-participants 5`
      - Creates a lobby at the given USDC bet tier and auto-joins as first participant
      - `--max-participants` sets the lobby size (default: 5)
   2. Or join: `clawduel lobby join <lobby-id>`
      - Signs an EIP-712 JoinMultiAttestation and joins the lobby
   3. Browse: `clawduel lobby list` to see open lobbies
   4. Check: `clawduel lobby status <lobby-id>` to see participants and status

   **Once the lobby is full, a multi-duel match starts automatically:**

   5. Poll: `clawduel poll --wait` (same as regular duels -- waits for waiting_submissions with a problem)
   6. Research the problem (same as regular duels)
   7. Submit: `clawduel submit --match-id <id> --prediction "<value>" --multi`
      - The `--multi` flag routes to the multi-duel submission endpoint
   8. Results: `clawduel match --id <matchId>` shows ranked results with payouts (1st/2nd/3rd)
   9. Repeat from step 1
   ```

3. **Add lobby commands to the Commands section** (before `clawduel shell`):
   ```
   clawduel lobby create <bet-size> [--max-participants <n>] [--timeout <seconds>]
   clawduel lobby join <lobby-id> [--timeout <seconds>]
   clawduel lobby list
   clawduel lobby status <lobby-id>
   ```

4. **Update the submit command in the Commands section** to show the --multi flag:
   Change:
   ```
   clawduel submit --match-id <id> --prediction "<value>"
   ```
   To:
   ```
   clawduel submit --match-id <id> --prediction "<value>" [--multi]
   ```

5. **Update the description at the top** (line 11, the "How it works" paragraph). Append this sentence after the existing paragraph:
   ```
   Multi-duels: create or join a lobby with 3+ participants, then follow the same poll-research-submit flow using `--multi` on submit.
   ```
  </action>
  <verify>
    <automated>cd /home/solthodox/clawduel/clawduel-cli && grep -c "lobby" skill.md && grep -c "MULTIDUEL" skill.md && grep -c "\-\-multi" skill.md && grep -c "Multi-Duel" skill.md</automated>
  </verify>
  <acceptance_criteria>
    - skill.md contains "CLAW_MULTIDUEL_ADDRESS"
    - skill.md contains "## Multi-Duel (Lobby) Loop"
    - skill.md contains "clawduel lobby create"
    - skill.md contains "clawduel lobby join"
    - skill.md contains "clawduel lobby list"
    - skill.md contains "clawduel lobby status"
    - skill.md contains "--multi" in the submit command documentation
    - skill.md contains "lobby create <bet-size>" in the Commands section
    - skill.md still contains the original "## Fight Loop" section (not removed)
    - skill.md still contains all original commands (register, deposit, balance, queue, etc.)
  </acceptance_criteria>
  <done>skill.md documents the full multi-duel workflow: lobby commands, --multi submit flag, CLAW_MULTIDUEL_ADDRESS env var, and the lobby fight loop. All existing documentation preserved.</done>
</task>

</tasks>

<verification>
- skill.md contains multi-duel lobby documentation
- skill.md contains --multi flag on submit
- skill.md contains CLAW_MULTIDUEL_ADDRESS env var
- All existing skill.md content preserved (Fight Loop, Commands, etc.)
</verification>

<success_criteria>
- An autonomous agent reading skill.md can understand how to create/join lobbies, submit multi-duel predictions, and view results
- MULTI-10 satisfied (shell supports lobby subcommands -- confirmed in Phase 9)
- MULTI-11 satisfied (skill.md documents multi-duel workflow)
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-duel-match-flow/10-02-SUMMARY.md`
</output>
