---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [readme-update]

must_haves:
  truths:
    - "README reflects global binary installation (npm install -g / npm link)"
    - "README documents --non-interactive init for agents"
    - "README documents multi-agent keystore support (--agent flag)"
    - "README documents queue --timeout flag"
    - "README references skill.md for autonomous agent usage"
    - "Commands section shows clawduel-cli binary (not just npx tsx)"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation reflecting v1.0 features"
      contains: "clawduel-cli init"
---

<objective>
Update README.md to reflect all v1.0 changes: global binary installation, agent key management (non-interactive init, multi-agent keystores, --agent flag), queue --timeout flag, and skill.md reference.

Purpose: README is stale -- it still shows `npx tsx clawduel-cli.ts` everywhere and is missing all v1.0 features (global binary, non-interactive init, multi-agent keystores, --timeout, skill.md).
Output: Updated README.md
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with v1.0 features</name>
  <files>README.md</files>
  <action>
Rewrite README.md with these changes (preserve the existing structure/sections but update content):

**Setup section:**
- Add global install option: `npm install -g @clawduel/agent-sdk` which provides the `clawduel-cli` binary
- Keep the git clone + npm link path as dev setup alternative
- Replace all `npx tsx clawduel-cli.ts` with `clawduel-cli` throughout the file
- Update init description to mention `--non-interactive` mode for agents
- Add `CLAW_KEY_PASSWORD` to env vars with note it enables fully non-interactive keystore decryption

**Commands section:**
- All commands use `clawduel-cli` not `npx tsx clawduel-cli.ts`
- Add `init --non-interactive` variant (reads AGENT_PRIVATE_KEY and CLAW_KEY_PASSWORD from env)
- Add `--agent <address>` flag examples for multi-agent usage (e.g. `clawduel-cli balance --agent 0xABC...`)
- Add `--timeout <seconds>` to the queue command example
- Keep all existing commands

**Multi-Agent section (new, after Commands):**
- Explain keystores stored at `~/.clawduel/keystores/<address>.json`
- Show `--agent 0x...` flag usage for switching between agents
- Note legacy `~/.clawduel/claw-keyfile.json` fallback still works

**Fight Loop section:**
- Update all commands to use `clawduel-cli` binary

**Agent Integration section (new, before SDK):**
- Point to `https://clawduel.ai/skill.md` for autonomous agent setup
- One-liner: "For AI agents (Claude Code, etc.), fetch the skill document and follow its instructions -- no human needed after initial setup."

**SDK section:**
- Keep as-is (still future/programmatic)

Do NOT add emojis. Keep the tone terse and developer-focused, matching the existing style.
  </action>
  <verify>
    <automated>grep -c "npx tsx" README.md | xargs test 0 -eq && grep -q "clawduel-cli init" README.md && grep -q "non-interactive" README.md && grep -q "skill.md" README.md && grep -q "\-\-agent" README.md && grep -q "\-\-timeout" README.md && echo "PASS"</automated>
  </verify>
  <done>README.md has zero occurrences of "npx tsx", documents global binary, non-interactive init, multi-agent keystores, --timeout flag, and skill.md reference</done>
</task>

</tasks>

<verification>
- `grep -c "npx tsx" README.md` returns 0
- `grep -c "clawduel-cli " README.md` returns 10+ (commands use binary)
- All v1.0 features mentioned: --non-interactive, --agent, --timeout, skill.md
- npm run build still succeeds (no code changes, just README)
</verification>

<success_criteria>
README.md accurately reflects the current v1.0 state of the CLI with global binary usage, agent key management features, and skill.md reference. No stale references to npx tsx remain.
</success_criteria>

<output>
After completion, create `.planning/quick/260318-u9d-update-readme/260318-u9d-SUMMARY.md`
</output>
